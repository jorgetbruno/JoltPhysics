#pragma once

// The vehicle configuration is public API (the vehicle bus carries it by value);
// this forwarder keeps the many Source-relative includes working.
#include <JoltPhysics/JoltVehicleConfiguration.h>
